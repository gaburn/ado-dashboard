# Project Context

- **Owner:** the team
- **Project:** wip-dashboard — Textual TUI for Azure DevOps work items and PRs
- **Stack:** Python 3.11+, Textual, terminal UX, keyboard-first interaction model
- **Created:** 2026-05-08
- **Cast:** Lord of the Rings — Thorin's Company

## Learnings

- Thorin completed contributor docs (2026-05-08): See `docs/configuration.md` for configuration layer resolution, schema, setup wizard, and in-app settings. See `docs/development.md` for UX conventions, CSS patterns, and known cruft tracker.
- **Dwalin Test Suite (2026-05-08):** 79 tests written for config, investigation, models, triage. Establishes pytest fixtures, autouse patterns for module state, and test conventions. See `src/tests/` and `.squad/decisions/decisions.md`.
- **App Rename (2026-07-17):** Updated all user-facing copy in `README.md`, `CONTRIBUTING.md`, and all `docs/*.md` from `wip-dashboard` / `WIP Dashboard` / `wip_dashboard` to `ado-dashboard` / `ADO Dashboard` / `ado_dashboard`. GitHub repo URLs and `cd wip-dashboard` directory references left pending repo rename (Thorin/user coordination). `WIP_DASHBOARD_REPO_ROOT` env var intentionally left as-is — name is determined by code (Thorin's scope).
- **Bofur Triage Empty-State + Notification Fix (2026-05-28):** Two UX problems when `triage_script_path` is unconfigured. (1) Suppressed the noisy `"Triage: triage_script_path is not configured"` toast that fired on app launch — when triage is optional and the user hasn't opted in, the notification is noise. Both `_fetch_and_populate_triage()` and `_refresh_triage()` in `dashboard.py` now short-circuit silently (INFO log only) when `config.TRIAGE_SCRIPT_PATH` is falsy; the notify path is preserved for real errors when a path IS set. (2) Branched the Triage tab empty state in `_populate_triage_groups()`: unconfigured → calm "No triage board configured. Add one in Settings if you'd like to use Triage." (no 🎉); configured-but-empty → keeps original celebratory "No items in triage queue 🎉". (3) Added a docs hint ("See docs/triage-and-investigation.md for setup instructions.") to the Triage Boards section of `settings.py` so anyone actively configuring triage finds the reference there. `s` key binding for Settings is already in the global footer — no new binding needed. See `.squad/decisions/inbox/bofur-triage-empty-state.md`.
- **Bofur UI Copy Audit (2026-07-17):** Fixed stale user-visible copy throughout `src/ado_dashboard/`:
  - `setup_wizard.py`: Banner "WIP Dashboard — First-Run Setup" → "ADO Dashboard — First-Run Setup"
  - `app.py`: Docstring "WIP Dashboard" → "ADO Dashboard" and `TITLE = "WIP Dashboard"` → `TITLE = "ADO Dashboard"`
  - `__main__.py`: Main function docstring "WIP Dashboard" → "ADO Dashboard"
  - `__init__.py`: Module docstring "WIP Dashboard" → "ADO Dashboard"
  - `screens/__init__.py`: Module docstring "WIP Dashboard" → "ADO Dashboard"
  - Intentionally left unchanged: class names (`WipDashboardApp`, `_wip_dark_theme`), theme identifier `"wip-dark"`, log file name `ado-dashboard.log`, config dir `ado-dashboard` (all stable identifiers per Thorin/Balin). Syntax validated via `py_compile`.
- **Bofur Sessions Tab Rename (2026-07-17):** Renamed "Sessions" tab to "Copilot Sessions" in all user-facing labels and documentation. Updated 7 strings across 4 files: (1) `dashboard.py`: TabPane label, keyboard binding label, docstring, and status bar text; (2) `app.py`: subtitle; (3) `README.md`: tab table header and features description; (4) `architecture.md`: process documentation. Left internal identifiers unchanged: `sessions_tab`, `_sessions`, `session_client.py`, CSS `/* Sessions table */` comment. Python compile check passed.
- **Bofur GitHub URL Slug Sweep (2026-05-15):** Renamed all GitHub URL references from `gaburn/wip-dashboard` to `gaburn/ado-dashboard` post-repo-rename. Swept 7 files: `README.md` (CI badge URL, local cd reference), `CONTRIBUTING.md` (clone URL, cd reference, GitHub Issues link), `SECURITY.md` (vulnerability advisory URL), `CODE_OF_CONDUCT.md` (GitHub Issues link), `docs/development.md` (cd reference). Replaced 6 URL references and 3 local directory references. Verified clean via re-grep on `gaburn/wip-dashboard`, `cd wip-dashboard`, and `wip-dashboard.git` patterns — all matches eliminated.
- **Bofur Public-Readiness Audit (2026-07-17):** Full audit of first-time visitor experience across README.md, CONTRIBUTING.md, SECURITY.md, CODE_OF_CONDUCT.md, issue templates, and PR template. Key findings: (🔴) README missing `git clone` step in Install & Run; no screenshots/GIF for a TUI (fatal for first impressions); CODE_OF_CONDUCT directs conduct reports to public GitHub Issues instead of a private channel (harmful); CONTRIBUTING.md does not mention the `.squad` AI team, which is visible in the repo tree and unexplained. (🟡) README hero sentence is thin on WHO/WHY; no Troubleshooting section; Architecture section too heavy for a user README; commit/branch conventions unspecified in CONTRIBUTING; SECURITY.md "supported versions" table awkward for a pre-1.0 project. Full report at `.squad/decisions/inbox/bofur-public-audit.md`.
- **Bofur Public-Prep Execution (2026-07-17):** Fixed all blockers and recommended items from public-readiness audit in one pass. Files changed: `README.md` (git clone step, screenshot TODO placeholder, hero sentence rewrite, ADO expansion on first use, architecture section collapsed to one-liner, Troubleshooting/FAQ section added), `CONTRIBUTING.md` (`.squad/` explanation, commit message conventions, branch naming table, pre-commit hook hint), `CODE_OF_CONDUCT.md` (conduct reports redirected from public Issues to private security advisory + placeholder maintainer email), `SECURITY.md` (supported versions table updated to "Latest release (0.x)"), `.github/ISSUE_TEMPLATE/feature_request.md` ("What workflow does this support?" field added), `.github/PULL_REQUEST_TEMPLATE.md` (checklist aligned with CONTRIBUTING comment policy), `docs/architecture.md` (Key Modules quick-reference table appended). Cannot-do: screenshot/GIF requires live app capture — placeholder comment added in README. Stale `wip-dashboard` grep clean on all edited files (CHANGELOG.md historical reference is intentional). See `.squad/decisions/inbox/bofur-public-prep-execution.md`.
- **Issue #3 Tracks Assigned (2026-05-28):** Thorin decomposed Issue #3 (work item field editing) into 4 sub-tracks. **Bofur track #3-c:** Edit form UX — layout, bindings, validation feedback. No blockers.
- **Bofur Issue #3 Edit UX Spec (2026-05-28):** Designed the Edit Work Item screen spec for fields Type, Title, State, Iteration Path, Area Path, and Description. Recommended a one-column full-screen Textual form, `e` to edit, `Ctrl+S` save, `Esc` cancel, `Tab`/`Shift+Tab` field movement, `Enter`/`Space` dropdown choice, `Ctrl+E` expanded Description, and `?` as an edit-help alias/open question alongside existing `h`. Type changes require a modal confirmation because ADO may clear type-specific fields or invalidate State. Plain-text Description is recommended in-terminal; rich text should remain browser/API territory unless Balin proves a safe conversion path. Full spec written to `.squad/decisions/inbox/bofur-issue-3-edit-ux.md` and posted to GitHub issue #3.

- **Bofur Issue #3 EditWorkItemScreen UX Review (2026-05-28):** Reviewed Thorin's `EditWorkItemScreen` against the Bofur UX spec. Verdict: five-field v1 is shippable with small polish. Applied two tiny fixes in `screens/edit.py`: user-facing Type copy now says "read-only in this version" instead of exposing issue #4, and save/conflict handlers set `_error_detail` before `_save_state = "error"` so the status row shows specific errors. Accepted Thorin's `ConflictResolverModal.render()` override for v1 despite loss of dim background; v2 should restore dim via a better Textual/test strategy. Review written to `.squad/decisions/inbox/bofur-issue-3-ux-review.md`. Targeted edit-screen tests: 19 passed.
