# Balin — Org URL Normalization Bug Fix

**Session:** Post-rename bug fixes (org URL normalization + wizard banner sweep)

## Work Completed

- **Bug:** Setup wizard was saving bare organization name instead of full Azure DevOps URI to config
- **Fix #1:** Added `_normalize_org_url()` helper function in `src/ado_dashboard/config.py`
  - Applied in `load_from_file()` for config load-time normalization
  - Applied in `apply_overrides()` for override-time normalization
- **Fix #2:** Patched `src/ado_dashboard/setup_wizard.py` to normalize org input at prompt time
- **Fix #3:** Repaired live config on disk (mangled ado_project field corrected)

## Impact

- Config now correctly persists Azure DevOps organization URIs
- Subsequent wizard prompts handle org input correctly
- Live deployment config repaired

## Files Modified

- `src/ado_dashboard/config.py`
- `src/ado_dashboard/setup_wizard.py`
